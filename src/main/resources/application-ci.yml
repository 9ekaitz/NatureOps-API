spring:     
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/natureops
    username: root
    password:
  jpa:
    hibernate: 
      ddl-auto: create

natureops:
  security:
    jwt:
      secret: secret
      issuer: natureops.eus